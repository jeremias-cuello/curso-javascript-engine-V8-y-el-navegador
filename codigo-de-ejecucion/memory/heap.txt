El memory heap es el lugar en donde se guardan los valores de las variables y las funciones.

Single Trhead (Synchronous)
Esto quiere decir que JS puede hacer una tarea a la vez, no mas. Tiene su ventajas y sus desventajas.

En el proceso del codigo de ejecución hay dos cosas muy importantes:
Memory Heap y Call Stack

En el Memory Heap las variables y funciones no se guardan linealmente ni numerica, Js guarda en diferentes partes.
